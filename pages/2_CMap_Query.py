"""Stage 2: CMap / L1000 Query - placeholder until this stage is built."""

import streamlit as st

st.set_page_config(page_title="ATLAS - CMap Query", layout="wide")

st.title("Stage 2 - CMap / L1000 Query")
st.info(
    "This stage hasn't been run yet. Once you build the CMap querying script "
    "and it writes results to `results/cmap/`, this page will display the "
    "ranked connectivity scores here."
)
